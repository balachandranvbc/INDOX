export interface BlogPost {
  title: string;
  excerpt: string;
  date: string;
  image?: string;
  link: string;
  category: string;
}

export interface MediaItem {
  source: string;
  title: string;
  date: string;
  link: string;
}